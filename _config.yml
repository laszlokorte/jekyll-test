# Site settings
title: Jekyll Test
email: me@laszlokorte.de
description: > # Auf dieser Seite teste ich Jekyll mit Github
baseurl: "" # the subpath of your site, e.g. /blog/
url: "http://jekyll.laszlokorte.de" # the base hostname & protocol for your site
twitter_username: jekyllrb
github_username:  jekyll

# Build settings
markdown: kramdown

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "default"
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "drafts"
    values:
      layout: "post"
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts"
    values:
      layout: "post"
